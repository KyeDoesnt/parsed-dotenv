{
  "name": "parsed-dotenv",
  "version": "0.0.1",
  "description": "parsed .env variables from the start.",
  "main": "lib/parsed-dotenv.js",
  "types": "lib/parsed-dotenv.d.ts",
  "repository": "https://github.com/KyeDoesnt/parsed-dotenv.git",
  "author": "Kye Cedar",
  "license": "MIT",
  "scripts": {
    "lib:build": "rm ./lib/* && tsc",
    "lib:run": "node .",
    "lib:dev": "tsc -w & nodemon ./lib/parsed-dotenv.js",
    "example:build": "rm ./example/dist/** && tsc -p ./example/tsconfig.json",
    "example:run": "node ./example/dist/main.js",
    "example:dev": "tsc -w -p ./example/tsconfig.json & nodemon ./example/dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^18.8.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
